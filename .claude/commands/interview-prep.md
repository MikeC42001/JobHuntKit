Slash-command alias for the `interview-prep` skill. Read `agents/CONTEXT.md` then
`agents/interview-prep.md` (both relative to this repo root) in full and follow them exactly,
using $ARGUMENTS as the company name to prep for.

This file is a pointer, not a copy — if the instructions ever look out of date here, the two
files above are the source of truth.
