Slash-command alias for the `cv-setup` skill. Read `agents/CONTEXT.md` then
`agents/cv-setup.md` (both relative to this repo root) in full and follow them exactly, using
$ARGUMENTS as the instruction's argument (a new fact for Update mode, or bootstrap material for
Bootstrap mode).

This file is a pointer, not a copy — if the instructions ever look out of date here, the two
files above are the source of truth.
