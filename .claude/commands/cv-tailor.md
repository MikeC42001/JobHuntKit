Slash-command alias for the `cv-tailor` skill. Read `agents/CONTEXT.md` then
`agents/cv-tailor.md` (both relative to this repo root) in full and follow them exactly, using
$ARGUMENTS as the instruction's argument (empty for the default scan, `all`/`not-sent`/`stale`,
`render`/`render <Company>`, a URL, a known company name, or pasted posting text).

This file is a pointer, not a copy — if the instructions ever look out of date here, the two
files above are the source of truth.
