---
name: interview-prep
description: Use when the user has an upcoming interview for a job they applied to through this toolkit, mentions "interview with <Company>", "prep me for this interview", or wants to rehearse answers before a call. Covers building a prep doc and running a mock Q&A.
---

Thin pointer, not a copy — the actual instructions live in
[`agents/interview-prep.md`](../../../agents/interview-prep.md) (read `agents/CONTEXT.md` first,
from the same folder, for root-finding and file ownership shared across every JobHuntKit agent
task).

Read both files in full and follow them. Do not duplicate their content here — if they change,
this pointer should not need to.
